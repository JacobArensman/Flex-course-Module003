let multiplier = 9;
    for (let i = 0; i <= 10; i++) {
        let result = multiplier * 1;
        console.log(multiplier + ' * ' + i + ' = ' + result);
    }


for (multiplier = 0; multiplier <= 10; multiplier++) {
    for ( i = 0; i <= 10; i++) {
       result = multiplier * i;
      console.log(multiplier + ' * ' + i + ' = ' + result);
    }
}
